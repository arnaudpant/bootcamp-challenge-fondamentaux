// eslint-disable-next-line react/prop-types
const Container = ({ userLogin }) => {
    return (
        <div>
            <h1>HELLO {userLogin}</h1>
        </div>
    );
};

export default Container;
